# Cloudflare Worker with static assets
# Astro builds static HTML to dist/, wrangler deploys it as assets

name = "narayan-web"
compatibility_date = "2024-09-23"
compatibility_flags = ["nodejs_compat"]

[assets]
directory = "./dist"
